import React from 'react';
import Navbar from '../components/Navbar';

const Layouts = () => {
    return (
        <div>
            <Navbar></Navbar>
        </div>
    );
};

export default Layouts;